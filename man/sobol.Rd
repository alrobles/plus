% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sobol.R, R/sobol_methods.R
\name{sobol}
\alias{sobol}
\alias{next_point}
\alias{skip_points}
\alias{reset_sobol}
\alias{generate_points}
\title{Sobol Sequence Generator}
\description{
Create and manipulate Sobol sequence generators for generating quasi-random
low-discrepancy sequences.
}
\usage{
sobol(dimension = 1L, seed = 0L)

next_point(sobol_object)

skip_points(sobol_object, n)

reset_sobol(sobol_object)

generate_points(sobol_object, n)
}
\arguments{
\item{dimension}{Integer between 1 and 10. The dimension of the Sobol sequence.}

\item{seed}{Integer. Optional initial position in the sequence. Default is 0.}

\item{sobol_object}{A sobol object created with \code{sobol()}.}

\item{n}{Integer. Number of points to skip or generate.}
}
\value{
\code{sobol()} returns a sobol S3 object.

\code{next_point()} returns a numeric vector of length \code{dimension}.

\code{skip_points()} returns the updated sobol object invisibly.

\code{reset_sobol()} returns the updated sobol object invisibly.

\code{generate_points()} returns a matrix with n rows and dimension columns.
}
\details{
Sobol sequences are quasi-random low-discrepancy sequences that provide better
coverage of the sample space compared to pseudo-random sequences. They are
particularly useful for:
\itemize{
  \item Quasi-Monte Carlo methods
  \item Numerical integration
  \item Sensitivity analysis
  \item High-dimensional sampling
}

The implementation uses direction numbers to generate points that are more
evenly distributed than random sampling. This can lead to faster convergence
in numerical integration and more efficient exploration of parameter spaces.
}
\examples{
# Create a 2-dimensional Sobol generator
gen <- sobol(dimension = 2)
print(gen)

# Generate a single point
point1 <- next_point(gen)
print(point1)

# Generate multiple points
gen2 <- sobol(dimension = 2)
points <- generate_points(gen2, n = 100)
plot(points, pch = 20, main = "First 100 points of 2D Sobol sequence")

# Skip ahead in the sequence
gen3 <- sobol(dimension = 3)
gen3 <- skip_points(gen3, 1000)
point <- next_point(gen3)  # This is the 1001st point

# Reset and start over
gen4 <- sobol(dimension = 2)
p1 <- next_point(gen4)
p2 <- next_point(gen4)
gen4 <- reset_sobol(gen4)
p1_again <- next_point(gen4)  # Same as p1
all.equal(p1, p1_again)

# Start from a specific position using seed
gen5 <- sobol(dimension = 2, seed = 500)
# This generator starts at position 500

# 3D visualization example
\dontrun{
gen3d <- sobol(dimension = 3)
points3d <- generate_points(gen3d, n = 1000)
library(scatterplot3d)
scatterplot3d(points3d, pch = 20, main = "1000 points of 3D Sobol sequence")
}
}
\seealso{
\code{\link{print.sobol}}, \code{\link{summary.sobol}}
}
