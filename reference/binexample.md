# List with 100 random values for binary classfication It contains a list with x and y values. x.has 100 instances and 30 features. y is a vector of 100 binary (1 and 0) values. This dataset is for test plus classification.

List with 100 random values for binary classfication It contains a list
with x and y values. x.has 100 instances and 30 features. y is a vector
of 100 binary (1 and 0) values. This dataset is for test plus
classification.

## Usage

``` r
data(binexample)
```

## Format

An object of class `"list"`;

## Examples

``` r
data(binexample)
```
