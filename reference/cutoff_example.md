# Example of cutoff threshold from plus model for use in the consturctor function new_plus

Example of cutoff threshold from plus model for use in the consturctor
function new_plus

## Usage

``` r
data(cutoff_example)
```

## Format

An object of class `"numeric"`; it has a numeric value of the model
threshold.

## Examples

``` r
data(cutoff_example)
```
